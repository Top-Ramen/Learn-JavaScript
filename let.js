// Can not use the let variable before the declaration
// console.log(name);
let nam = 'Volkswagon';
let ag;
console.log(ag)

{
    let a = 10
}
// Now, a is undefined, because a is a let variable which is having a local or block level scope. 
// This variable can be used inside the block, but not outside the block.

console.log(a)
